import { TemplateInfo } from "../types";
import { TokenTypes } from "../constants";
import { PartToken, TemplateToken, TokenizerState } from "../types";
export declare const getRange: (info: TemplateInfo) => number[];
export declare function createParts<T extends TokenTypes>(state: TokenizerState, type: T): (PartToken<T> | TemplateToken<T>)[];
