"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/adapters/test-suite.js
var test_suite_exports = {};
__export(test_suite_exports, {
  runAdapterTestSuite: () => runAdapterTestSuite,
  testAdapterCompliance: () => testAdapterCompliance
});
module.exports = __toCommonJS(test_suite_exports);
var import_node_test = require("node:test");
var import_strict = __toESM(require("node:assert/strict"), 1);

// src/adapters/adapter.js
var REQUIRED_METHODS = [
  "isElement",
  "getTagName",
  "getAttribute",
  "hasAttribute",
  "getAttributeNames",
  "getTextContent",
  "getChildren",
  "getParent",
  "getSiblings",
  "stringify"
];
function validateAdapter(adapter) {
  if (!adapter || typeof adapter !== "object") {
    throw new Error("Adapter must be an object");
  }
  for (const method of REQUIRED_METHODS) {
    if (typeof /** @type {Record<string, any>} */
    adapter[method] !== "function") {
      throw new Error(`Adapter missing required method: ${method}()`);
    }
  }
}

// src/adapters/test-suite.js
function runAdapterTestSuite(AdapterClass, options) {
  const { createElement, supportsLocation = false } = options;
  if (!createElement) {
    throw new Error("createElement function is required in test options");
  }
  let adapter;
  function setup() {
    adapter = new AdapterClass();
  }
  (0, import_node_test.describe)("isElement", () => {
    (0, import_node_test.it)("should return true for valid element nodes", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.isElement(el), true, "Should identify valid element");
    });
    (0, import_node_test.it)("should return false for null", () => {
      setup();
      import_strict.default.equal(adapter.isElement(null), false, "Should return false for null");
    });
    (0, import_node_test.it)("should return false for undefined", () => {
      setup();
      import_strict.default.equal(adapter.isElement(void 0), false, "Should return false for undefined");
    });
  });
  (0, import_node_test.describe)("getTagName", () => {
    (0, import_node_test.it)("should return lowercase tag name for meta", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.getTagName(el), "meta");
    });
    (0, import_node_test.it)("should return lowercase tag name for LINK (uppercase HTML)", () => {
      setup();
      const el = createElement('<LINK rel="stylesheet" href="styles.css">');
      import_strict.default.equal(adapter.getTagName(el), "link");
    });
    (0, import_node_test.it)("should return lowercase tag name for script", () => {
      setup();
      const el = createElement('<script src="app.js"></script>');
      import_strict.default.equal(adapter.getTagName(el), "script");
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const result = adapter.getTagName(null);
      import_strict.default.equal(typeof result, "string", "Should return a string");
    });
  });
  (0, import_node_test.describe)("getAttribute", () => {
    (0, import_node_test.it)("should get attribute value", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.getAttribute(el, "charset"), "utf-8");
    });
    (0, import_node_test.it)("should be case-insensitive for attribute names", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.getAttribute(el, "CHARSET"), "utf-8");
      import_strict.default.equal(adapter.getAttribute(el, "CharSet"), "utf-8");
    });
    (0, import_node_test.it)("should return null for missing attribute", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.getAttribute(el, "name"), null);
    });
    (0, import_node_test.it)("should handle complex attribute values", () => {
      setup();
      const el = createElement('<meta http-equiv="Content-Security-Policy">');
      import_strict.default.equal(adapter.getAttribute(el, "http-equiv"), "Content-Security-Policy");
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      import_strict.default.equal(adapter.getAttribute(null, "charset"), null);
    });
  });
  (0, import_node_test.describe)("hasAttribute", () => {
    (0, import_node_test.it)("should return true when attribute exists", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.hasAttribute(el, "charset"), true);
    });
    (0, import_node_test.it)("should return false when attribute does not exist", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.hasAttribute(el, "name"), false);
    });
    (0, import_node_test.it)("should be case-insensitive", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      import_strict.default.equal(adapter.hasAttribute(el, "CHARSET"), true);
      import_strict.default.equal(adapter.hasAttribute(el, "CharSet"), true);
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      import_strict.default.equal(adapter.hasAttribute(null, "charset"), false);
    });
  });
  (0, import_node_test.describe)("getAttributeNames", () => {
    (0, import_node_test.it)("should return all attribute names", () => {
      setup();
      const el = createElement('<meta charset="utf-8" name="viewport">');
      const names = adapter.getAttributeNames(el);
      import_strict.default.ok(Array.isArray(names), "Should return an array");
      import_strict.default.ok(names.includes("charset"), "Should include charset");
      import_strict.default.ok(names.includes("name"), "Should include name");
    });
    (0, import_node_test.it)("should return empty array for element with no attributes", () => {
      setup();
      const el = createElement("<title>Test</title>");
      const names = adapter.getAttributeNames(el);
      import_strict.default.deepEqual(names, []);
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const names = adapter.getAttributeNames(null);
      import_strict.default.ok(Array.isArray(names), "Should return an array");
      import_strict.default.equal(names.length, 0, "Should be empty for null");
    });
  });
  (0, import_node_test.describe)("getTextContent", () => {
    (0, import_node_test.it)("should get text content from title", () => {
      setup();
      const el = createElement("<title>Hello World</title>");
      import_strict.default.equal(adapter.getTextContent(el), "Hello World");
    });
    (0, import_node_test.it)("should get text content from inline script", () => {
      setup();
      const el = createElement('<script>console.log("test");</script>');
      import_strict.default.equal(adapter.getTextContent(el), 'console.log("test");');
    });
    (0, import_node_test.it)("should get text content from inline style", () => {
      setup();
      const el = createElement("<style>body { margin: 0; }</style>");
      import_strict.default.equal(adapter.getTextContent(el), "body { margin: 0; }");
    });
    (0, import_node_test.it)("should return empty string for empty element", () => {
      setup();
      const el = createElement("<title></title>");
      import_strict.default.equal(adapter.getTextContent(el), "");
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const result = adapter.getTextContent(null);
      import_strict.default.equal(typeof result, "string", "Should return a string");
    });
  });
  (0, import_node_test.describe)("getChildren", () => {
    (0, import_node_test.it)("should return array of child elements", () => {
      setup();
      const el = createElement('<noscript><link rel="stylesheet" href="noscript.css"></noscript>');
      const children = adapter.getChildren(el);
      import_strict.default.ok(Array.isArray(children), "Should return an array");
      import_strict.default.equal(children.length, 1, "Should have 1 child");
      import_strict.default.equal(adapter.getTagName(children[0]), "link", "Child should be link element");
    });
    (0, import_node_test.it)("should return empty array for element with no children", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      const children = adapter.getChildren(el);
      import_strict.default.deepEqual(children, []);
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const children = adapter.getChildren(null);
      import_strict.default.ok(Array.isArray(children), "Should return an array");
      import_strict.default.equal(children.length, 0, "Should be empty for null");
    });
  });
  (0, import_node_test.describe)("getParent", () => {
    (0, import_node_test.it)("should return parent element", () => {
      setup();
      const head = createElement('<head><meta charset="utf-8"></head>');
      const children = adapter.getChildren(head);
      if (children.length > 0) {
        const meta = children[0];
        const parent = adapter.getParent(meta);
        import_strict.default.ok(parent, "Should return a parent");
        import_strict.default.equal(adapter.getTagName(parent), "head");
      } else {
        import_strict.default.ok(true, "Skipped - createElement does not support nested structures");
      }
    });
    (0, import_node_test.it)("should return null for element without parent", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      const parent = adapter.getParent(el);
      import_strict.default.ok(parent === null || adapter.isElement(parent), "Should return null or element");
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const parent = adapter.getParent(null);
      import_strict.default.equal(parent, null);
    });
  });
  (0, import_node_test.describe)("getSiblings", () => {
    (0, import_node_test.it)("should return sibling elements when they exist", () => {
      setup();
      const head = createElement('<head><meta charset="utf-8"><title>Test</title><link rel="icon"></head>');
      const children = adapter.getChildren(head);
      if (children.length >= 2) {
        const meta = children[0];
        const siblings = adapter.getSiblings(meta);
        import_strict.default.ok(Array.isArray(siblings), "Should return an array");
        import_strict.default.ok(siblings.length > 0, "Should have siblings");
        import_strict.default.ok(!siblings.includes(meta), "Should not include element itself");
      } else {
        import_strict.default.ok(true, "Skipped - createElement does not support nested structures");
      }
    });
    (0, import_node_test.it)("should not include the element itself", () => {
      setup();
      const head = createElement('<head><meta charset="utf-8"><title>Test</title></head>');
      const children = adapter.getChildren(head);
      if (children.length >= 2) {
        const meta = children[0];
        const siblings = adapter.getSiblings(meta);
        import_strict.default.ok(!siblings.includes(meta), "Should not include element itself");
        import_strict.default.ok(!siblings.some((s) => s === meta), "Should not contain element itself");
      } else {
        import_strict.default.ok(true, "Skipped - createElement does not support nested structures");
      }
    });
    (0, import_node_test.it)("should return empty array for element without siblings", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      const siblings = adapter.getSiblings(el);
      import_strict.default.ok(Array.isArray(siblings), "Should return an array");
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const siblings = adapter.getSiblings(null);
      import_strict.default.ok(Array.isArray(siblings), "Should return an array");
      import_strict.default.equal(siblings.length, 0, "Should be empty for null");
    });
  });
  (0, import_node_test.describe)("getLocation", () => {
    if (supportsLocation) {
      (0, import_node_test.it)("should return location object with line and column", () => {
        setup();
        const el = createElement('<meta charset="utf-8">');
        const loc = adapter.getLocation(el);
        import_strict.default.ok(loc !== null, "Should return location object");
        import_strict.default.ok(typeof loc === "object", "Location should be an object");
        import_strict.default.ok("line" in loc || "start" in loc, "Should have line or start property");
      });
    } else {
      (0, import_node_test.it)("should return null (location not supported)", () => {
        setup();
        const el = createElement('<meta charset="utf-8">');
        import_strict.default.equal(adapter.getLocation(el), null);
      });
    }
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      if (typeof adapter.getLocation === "function") {
        import_strict.default.equal(adapter.getLocation(null), null);
      }
    });
  });
  (0, import_node_test.describe)("stringify", () => {
    (0, import_node_test.it)("should stringify element with single attribute", () => {
      setup();
      const el = createElement('<meta charset="utf-8">');
      const str = adapter.stringify(el);
      import_strict.default.ok(str.includes("meta"), "Should include tag name");
      import_strict.default.ok(str.includes("charset"), "Should include attribute name");
      import_strict.default.ok(str.includes("utf-8"), "Should include attribute value");
    });
    (0, import_node_test.it)("should stringify element with multiple attributes", () => {
      setup();
      const el = createElement('<link rel="stylesheet" href="styles.css">');
      const str = adapter.stringify(el);
      import_strict.default.ok(str.includes("link"), "Should include tag name");
      import_strict.default.ok(str.includes("rel"), "Should include rel attribute");
      import_strict.default.ok(str.includes("href"), "Should include href attribute");
    });
    (0, import_node_test.it)("should stringify element with no attributes", () => {
      setup();
      const el = createElement("<title>Test</title>");
      const str = adapter.stringify(el);
      import_strict.default.ok(str.includes("title"), "Should include tag name");
    });
    (0, import_node_test.it)("should handle null node gracefully", () => {
      setup();
      const str = adapter.stringify(null);
      import_strict.default.equal(typeof str, "string", "Should return a string");
    });
  });
}
function testAdapterCompliance(AdapterClass) {
  (0, import_node_test.describe)("Adapter Compliance", () => {
    (0, import_node_test.it)("should implement all required methods", () => {
      const adapter = new AdapterClass();
      import_strict.default.doesNotThrow(() => validateAdapter(adapter));
    });
    (0, import_node_test.it)("should be instantiable without errors", () => {
      import_strict.default.doesNotThrow(() => {
        new AdapterClass();
      });
    });
  });
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  runAdapterTestSuite,
  testAdapterCompliance
});
